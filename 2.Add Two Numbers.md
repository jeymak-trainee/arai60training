add two numbers

## step1  
以下思ったこと  
久しぶりに練習を再開したので、javaの文法などほとんどわすれていた  
l1とl2のノードに対して、計算結果のノードを前のノードとリンクさせ、繰り上がりと次の計算対象を渡すとこまでは考えられた  
しかし、ループをどうやって正しく回せばいいのかわからなくなり、やむを得ずchatgptに聞きながらなんとか完成させた  
どこか別の場所で小田さんがコメントしていたが、ループの不変条件は次のループに対する約束だという観点は印象に残った  
変数名にsumを使ってるが、後の練習でtotalに直した  
変数名にcurrentをつけているが、単純にnodeだけでよかったかも  
```Java
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        ListNode dummynode = new ListNode(0);
        ListNode currentnode = dummynode;
        int carryup =0;
        while (l1 != null || l2 != null || carryup != 0) {
            int l1value = (l1 != null) ? l1.val : 0;
            int l2value = (l2 != null) ? l2.val : 0;
            int sum = l1value + l2value + carryup;
            currentnode.next = new ListNode(sum % 10);
            carryup = sum / 10;
            currentnode = currentnode.next;
            l1 = (l1 != null) ? l1.next : null;
            l2 = (l2 != null) ? l2.next : null;
        }
        return dummynode.next;
    }
}
```




## step2

### 番兵なしの再帰  
思ったこと  
計算結果のノードを最後の桁まで作成した後、帰りがけの処理でどんどんリンクしていくイメージを持った  
どこで見かけたコメントかわからなくなってしまったが、helperの引数の並びが加算器のそれと同様でいいですねという旨のコメントがあった  
そんなイメージは全く持っていなかったので印象に残った

```Java 
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        int carryup = 0;
        return addTwoNumbershelper(l1, l2, carryup);
    }
    private ListNode addTwoNumbershelper(ListNode l1, ListNode l2, int carryup) {
        if (l1 == null && l2 == null && carryup == 0) {
            return null;
        }
        int l1value = (l1 != null) ? l1.val : 0;
        int l2value = (l2 != null) ? l2.val : 0;
        int sum = l1value + l2value + carryup;
        carryup = sum / 10;
        ListNode node = new ListNode(sum % 10);
        l1 = (l1 != null) ? l1.next : null;
        l2 = (l2 != null) ? l2.next : null;
        node.next = addTwoNumbershelper(l1, l2, carryup);
        return node;
    }
}


```





### 末尾再帰で実装  
思ったこと  
再帰の各段階で計算結果のノードをリンクさせることで、帰りがけの処理をなくしている  
最初にダミーを作成して渡すことでこれが可能になっている  
番兵はforやwhileループを回す時くらいしか使わないのだと勝手に思っていたが、ループでも再帰でも活用できることを知れた  
pythonで書いたコードに対して、sumは組み込み関数と同じ名前だからやめたほうがいいというコメントを見かけた  
調べたらJavaでも同名のAPIがいろいろ用意されていたので、代わりに`total`にすることにした  
https://docs.oracle.com/javase/8/docs/api/java/lang/Integer.html  


```java
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        int carry = 0;
        ListNode head = new ListNode(0);
        addTwoNumbershelper(head, l1, l2, carry);
        return head.next;       
    }
    private void addTwoNumbershelper(ListNode prev, ListNode l1, ListNode l2, int carry) {
        if (l1 == null && l2 == null && carry == 0) {
            return ;
        }
        int l1value = (l1 != null) ? l1.val : 0;
        int l2value = (l2 != null) ? l2.val : 0;
        int total = l1value + l2value + carry;
        carry = total / 10;
        prev.next = new ListNode(total % 10);
        l1 = (l1 != null) ? l1.next : null;
        l2 = (l2 != null) ? l2.next : null;
        addTwoNumbershelper(prev.next, l1, l2, carry);
    }
}


```
