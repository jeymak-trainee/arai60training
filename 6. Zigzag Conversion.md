## 参考  
https://github.com/katsukii/leetcode/pull/7/files
Javaで自分と同様の方針でやってた。Solution2は自分がやりたかったけどうまくできなかった方法なので参考にしたい。  

https://github.com/saagchicken/coding_practice/pull/22/files
Pythonで、generatorを使って解いてた。generatorを使うと処理がシンプルにかけて、大変読みやすいと感じた。ただ、処理は重くなるらしい。  


## Step1  
文字の配置の仕方が階段の上り下りと似ているなと思って、とりあえず手続きを書いてみたら通った。  


```Java

class Solution {
    public String convert(String s, int numRows) {
        if (numRows == 1) return s;
        List<StringBuilder> rows = new ArrayList<>();
        for (int i = 0; i < numRows; i++) {
            rows.add(new StringBuilder());
        }
        int currentRow = 1;
        int bottomRow = numRows;
        int topRow = 1;
        boolean goDown = true;
        for (char c : s.toCharArray()) {
            rows.get(currentRow - 1).append(c);
            if ((currentRow == bottomRow) && (goDown)) {
                goDown = false;
            } else if ((currentRow == topRow) && (!goDown)) {
                goDown = true;
            }
            currentRow += goDown ? 1 : -1;
        }
        StringBuilder result = new StringBuilder();
        for (StringBuilder row : rows) {
            result.append(row);
        }
        return result.toString();
    }
}

```

`StringBuilder`の仕様を全然知らないなと思い、調べてみた。 
 https://docs.oracle.com/javase/8/docs/api/java/lang/StringBuilder.html
`StringBuilder`は指定がなければデフォルトで16個の文字を格納でき、それ以上の個数の文字を挿入しようとすると内部でリサイズ処理が自動で実行される。容量拡張時は倍々で大きくしていくため、容量`n`までの拡張回数は大体`log(n)`回で済む。  
あとメソッド一覧を見ていて初めて気づいたが、`StringBuilder`は任意の型を受け取り文字列へ変換できる。単に文字列を放り込めば結合してくれる、ちょっと便利なクラスだと思っていた。`printf`の強化版みたいな印象を受けた。    
次は別のやり方でジグザグを処理してみる。  

## Step2  
ジグザグをサイクルで区切る。サイクルは、`topRow --> bottomRow --> topRow`と戻ってくるまでを1単位としている。サイクルの長さは`numRows * 2 - 2`。  

```Java

class Solution {
    public String convert(String s, int numRows) {
        if ((numRows == 1) || (s.length() < numRows)) return s;
        StringBuilder[] rows = new StringBuilder[numRows];
        for (int i = 0;  i < numRows; i++) {
            rows[i] = new StringBuilder();
        }
        int cycleLength = numRows * 2 - 2;
        for (int i = 0; i < s.length(); i++) {
            int rowIndex = i % cycleLength;
            if (rowIndex >= numRows) rowIndex = cycleLength - rowIndex;
            rows[rowIndex].append(s.charAt(i));
        }
        StringBuilder result = new StringBuilder(s.length());
        for (StringBuilder sb : rows) {
            result.append(sb);
        }
        return result.toString();
}
}

```
別にコピペしたとかではないが、参考にしたコードとほとんど同じになってしまったように見える。大丈夫なのかどうかわからない。  


参考にしたコードでは、`StringBuilder result = new StringBuilder(s.length());`とコンストラクタに初期容量を指定していた。必要な容量は問題の設定から明らかなので、その値をコンストラクタに渡せば容量拡張処理を行わずに済む。ちょっとしたことかもしれないが、自分が最初書いたときはできていなかったし、できたほうがいいと思うので気を付ける。一方で、最初に`rows`を用意するときも`StringBuilder()`のコンストラクタを初期容量指定なしで呼んでいる。しかし、ここでも初期容量を指定してしまうとその後の処理を変更しづらくなるかもしれないと思い、そのままにした。  

あと、配列の宣言で`StringBuilder[]`と書けるのを知らなかった。いままで、配列はプリミティブ型しか宣言できず、クラスを要素に持ちたいなら`List<StringBuilder>`とか使わないとだめだと思い込んでいた。仕様書を読んでみる。  
https://docs.oracle.com/javase/specs/jls/se21/html/jls-10.html
`List[]`のようにインターフェースを書いてもいいし、抽象型を書いてもいいらしい。全然知らなかった。   


## 両方やってみた感想  
Step1の方法だと、ジグザグの形が途中で変化する状況に対応しやすそうだなと思った。一方、Step2の方法だととても長い`s`が与えられた時でも、分割して処理できるようにプログラムを変更しやすそうな気がした。ジグザグの1サイクルが長いなら`rowIndex`を先に計算して配列として用意し、`append(s.charAt(i))`を並行して処理するとか（変なこと言ってるかもしれない）、いろいろな変更方法がありそうだなと感じた。
