# step1で思ったこと  
与えられたストリングを、1日1文字だけ先頭から調べられる  
明日の自分（他人）に作業を引き継ぐには何を記録する必要があるか？  
現在の部分文字列を記録して引き継ぐ必要がある  
基本はwindowの末尾に調べた文字を追加し、そのwindowを引き継ぎとして渡す  


windowに今調べた文字が含まれているかを確認するために、いちいちwindowを走査するのはめんどくさい感じがする  
めんどくさいというのは、windowが長くなるとちょっと無視できない時間がかかりそうという意味  
そこで、windowに含まれてる文字のリストisInWindowを作成して、これも引き継ぐようにする


上記のイメージをコードに起こしてみる  
文法はあまりわからないのでchatgptに聞きながら書く  

```Java

class Solution {
    public int lengthOfLongestSubstring(String s) {
        StringBuilder window = new StringBuilder();
        final int ASCII_CHARACTER = 128;
        boolean[] isInWindow = new boolean[ASCII_CHARACTER];
        int maxLength = 0;
        for (char c : s.toCharArray()) {
            if (isInWindow[c]) {
                maxLength = Math.max(maxLength, window.length());
                int position = window.indexOf(String.valueOf(c));
                for (int i = 0; i <= position; i++) {
                    isInWindow[window.charAt(i)] = false;
                }
                window.delete(0, position + 1);
            }
            window.append(c);
            isInWindow[c] = true;
        }
        return Math.max(maxLength, window.length());
    }
}

```

入力に絵文字とか来た場合、boolean[]では対応できない  
辞書を使用するように修正  
```Java

class Solution {
    public int lengthOfLongestSubstring(String s) {
        StringBuilder window = new StringBuilder();
        HashMap<Character, Boolean> isInWindow = new HashMap<>();
        int maxLength = 0;
        for (char c : s.toCharArray()) {
            if (isInWindow.getOrDefault(c, false)) {
                maxLength = Math.max(maxLength, window.length());
                int point = window.indexOf(String.valueOf(c)); 
                for (int i = 0; i < point + 1; i++) {
                    isInWindow.put(window.charAt(i), false);
                }
                window.delete(0, point + 1);
            }
            window.append(c);
            isInWindow.put(c, true);
        }
        return Math.max(maxLength, window.length());
    }
}

```
短いコードだから修正はなんとかできた。しかし、規模が大きくなると、今のやり方ではコードの修正を抜けもれなくやるのが難しくなりそうな印象がある。


# 感想  
ここしばらくは協会のコーディング練習から離れて、明日の自分に作業を引き継ぐならどうするか、という観点で会社の業務（ほとんどコードは書かないが）に取り組んでいた。  
久しぶりにコードを書いてみると、仕事の段取りとコードを書く時の頭の使い方がほぼ同じ風になっているのに気が付いた。  
コードの拙さがそのまま自分の仕事の拙さを反映しているようで正直恥ずかしい。今まで結構恥ずかしいことをしていたかもしれないと思うと、胃がキュッとなる。
